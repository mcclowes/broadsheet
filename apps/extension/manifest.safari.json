{
  "_comment": "Safari-specific overrides. Deep-merged onto manifest.json by scripts/package.mjs when --target=safari to produce the source bundle fed to `xcrun safari-web-extension-converter`. Safari 16.4+ supports MV3 service workers, so background.service_worker is inherited from the base manifest unchanged.",
  "browser_specific_settings": {
    "safari": {
      "strict_min_version": "16.4"
    }
  }
}
